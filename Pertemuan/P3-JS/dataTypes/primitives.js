// string  
let address = "Yogyakarta"; // bisa pake '' "" ``
console.log(address);

// number
let score = 80;
console.log(score);

// Boolean
let isRainy = true; // true or false
console.log(isRainy);

// Null 
let nullable = null;
console.log(nullable);

// undefined
let undefinedVar = undefined; // undefined
console.log(undefined);

// Notes: 
// null, undefined is same , null is for variable taht null or empty
// undefined is variable that has not defined and it is the same to boolean false
// if we use condition